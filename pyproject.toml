[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "quantfolio"
version = "0.1.0"
description = "A portfolio analytics platform for retail investors"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["quantfolio*"]